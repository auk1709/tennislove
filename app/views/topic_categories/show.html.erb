<div class="row">
  <div class="col-sm-offset-2 col-sm-8">
    <h2>一覧</h2>
    <%= link_to '新規作成', new_topic_path %>
    <ul class="list-group">
      <% @topics.each do |topic| %>
        <li class="list-group-item">
          <h3><%= link_to topic.title, topic_path(topic) %></h3>
        </li>
      <% end %>
    </ul>
  </div>
</div>